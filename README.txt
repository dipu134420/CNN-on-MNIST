In this project, I trained an MLP to classify images from the MNIST hand-written digit database.

The steps for this projects are: 
1. Load and visualize the data
2. Define a neural network
3. Train the model
4. Evaluate the performance of the trained model on a test dataset